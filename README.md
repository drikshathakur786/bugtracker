# Bug Tracker

Full-stack Bug & Task Tracker built with Spring Boot + React.

## Structure
- `/backend` — Spring Boot 3, Java 17, PostgreSQL
- `/frontend` — React 18, Vite, Axios

## Running locally
- Backend: open `/backend` in IntelliJ and run BugtrackerApplication
- Frontend: cd into `/frontend` and run `npm run dev`